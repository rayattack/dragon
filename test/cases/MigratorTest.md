# MigratorTest cases

Source programs for `test/MigratorTest.cpp`. Each block is named by the heading
above it and pulled in with `code("<name>")`.

#### :nested_blocks

```py
def foo():
    if True:
        return 1
    return 0
```

#### :class_with_method

```py
class Dog:
    def bark(self):
        print("Woof")
```

#### :for_loop_with_body

```py
for i in range(5):
    print(i)
```

#### :try_catch

```py
try:
    pass
except:
    pass
```
